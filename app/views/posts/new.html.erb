<h1>新規投稿</h1>

<%= form_with model: @post do |f| %>

  <h4>ホテル名</h4>
  <%= f.text_field :title %>
  <h4>宿泊</h4>
  <%= f.text_area :body %>
  <h4>画像</h4>
  <%= f.file_field :images, multiple: true %>
  <%= f.submit '投稿' %>
<% end %>